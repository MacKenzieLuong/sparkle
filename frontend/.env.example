# false connects to the backend; true enables the standalone layout demo.
VITE_MOCK_MODE=false
# Vite development proxy target. For the car, use http://<pi-address>:8000.
ROBOT_API_URL=http://127.0.0.1:8000
# Layout demo only. Live keywords come from backend /capabilities.
VITE_STOP_WORD=stop
